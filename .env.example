# TMDB (The Movie Database) - https://www.themoviedb.org/settings/api
REACT_APP_TMDB_API_KEY=twoj_klucz_tmdb

# OMDb API - https://www.omdbapi.com/apikey.aspx
REACT_APP_OMDB_API_KEY=twoj_klucz_omdb

# Supabase - https://supabase.com → Settings → API
# Tabela: CREATE TABLE saved_movies (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE, movie_id integer NOT NULL, media_type text NOT NULL DEFAULT 'movie', created_at timestamptz DEFAULT now());
REACT_APP_SUPABASE_URL=https://twoj-projekt.supabase.co
REACT_APP_SUPABASE_ANON_KEY=twoj_anon_key_supabase
